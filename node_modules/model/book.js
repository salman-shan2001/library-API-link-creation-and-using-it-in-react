const mongoose=require("mongoose")
const schema=mongoose.schema(
    {
        "title":String,
        "author":String,
        "description":String,
        "edition":String,
        "prize":String
    }
)

const librarymodel=mongoose.model("books",schema);
model.exports={librarymodel}

